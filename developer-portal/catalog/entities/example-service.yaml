apiVersion: platform.internal/v1

kind: Service

metadata:
  name: example-api

spec:

  owner: engineering-team

  description: Example managed platform service

  repository:
    url: github.com/example/example-api

  lifecycle:
    status: production
